experiment:
  name: mamba_unet_v0
  out_dir: artifacts/exp

data:
  sample_rate: 16000

model:
  name: mamba_unet
  in_ch: 1
  base: 32

train:
  epochs: 3
  batch_size: 8
  lr: 1.0e-3

inference:
  frame_ms: 20
  seconds: 1
  out_wav: artifacts/demo_out.wav

export:
  name: mamba_unet_v0
  out_dir: artifacts/export
  onnx:
    opset: 17