[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
mv pyproject.merged.toml pyproject.toml
rm pyproject.build-system.toml

pip install -e .
python -m ultra_se_slim.train ...




